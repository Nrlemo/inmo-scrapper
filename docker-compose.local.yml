# Sólo para probar en tu máquina: sin authentik, escucha en 127.0.0.1.
services:
  web:
    ports: ["127.0.0.1:8000:8000"]
    environment:
      AUTH_DISABLED: "true"
      AUTH_DEFAULT_USER: ${AUTH_DEFAULT_USER:-local}
